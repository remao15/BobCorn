# Copy this file to .env.local and fill in your key.
# .env.local is gitignored.
#
# Get a key at https://openrouter.ai/keys
# The key is bundled into the frontend, so for production
# move this call into a Supabase Edge Function instead.

VITE_OPENROUTER_KEY=sk-or-YOUR_KEY_HERE
