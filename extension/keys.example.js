/**
 * CartCop API Key Setup
 *
 * DO NOT commit keys.js — it is gitignored.
 *
 * To configure your OpenRouter API key:
 * 1. Copy this file to extension/keys.js
 * 2. Replace the placeholder with your real key from https://openrouter.ai/keys
 * 3. Reload the extension in chrome://extensions
 *
 * Alternatively, paste this one-liner into the service worker console
 * (chrome://extensions → CartCop → Inspect service worker):
 *
 *   chrome.storage.local.set({ openrouterKey: 'sk-or-YOUR_KEY_HERE' })
 */
